# Server Configuration
PORT=4000
NODE_ENV=development

# CORS - Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://127.0.0.1:5173

# JWT Configuration
# Generate secrets with: openssl rand -hex 64
JWT_ACCESS_SECRET=your-access-secret-here-minimum-64-characters-long-and-very-secure
JWT_REFRESH_SECRET=your-refresh-secret-here-minimum-64-characters-long-and-very-secure
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Admin Seed Account
ADMIN_EMAIL=admin@upalit.com
ADMIN_PASSWORD=ChangeMe123!
ADMIN_NAME=System Administrator

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=20